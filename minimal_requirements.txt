# minimal_requirements.txt - Python 3.13 Compatible (Phase 1 Only)

# Essential for scraping
requests==2.31.0
beautifulsoup4==4.12.2

# Use html.parser instead of lxml to avoid compilation issues
# (BeautifulSoup will fallback automatically)

# For embeddings - use transformers directly (more stable)
transformers==4.44.2
torch==2.1.2

# Basic data processing
numpy==1.26.4

# Skip problematic packages for now:
# - lxml (use html.parser)
# - scikit-learn (we'll implement TF-IDF manually)  
# - psycopg2 (Phase 2 concern)
# - pydantic (Phase 2 concern)